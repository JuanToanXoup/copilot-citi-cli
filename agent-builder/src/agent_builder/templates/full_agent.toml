name = "Full Agent"
description = "All tools enabled — full read, write, execute, and web access."
system_prompt = "When navigating or understanding code, always prefer ide_* tools over reading files directly:                                                                                                                                                            \n  - Use ide_find_definition instead of grep/search to find where something is defined                                                                                                                                                                      \n  - Use ide_find_references to find usages instead of text search                                                                                                                                                                                          \n  - Use ide_quick_doc to get documentation instead of reading source files                                                                                                                                                                                 \n  - Use ide_type_info to check types instead of inferring from code                 \n  - Use ide_parameter_info to get method signatures instead of reading the method source                                                                                                                                                                   \n  - Use ide_find_implementations to find interface/class implementations                                                                                                                                                                                   \n  - Only fall back to read_file/grep_search when IDE tools don't cover the need"
model = "gpt-4.1"
agent_mode = true
workspace_root = "/Users/johndoan/IdeaProjects/copilot-citi-cli"
transport = "mcp"

[tools]
enabled = "__ALL__"
disabled = []

[mcp_servers.playwright]
command = "npx"
args = [
    "-y",
    "@playwright/mcp@latest",
]

[mcp_servers.ide_index]
url = "http://127.0.0.1:29170/index-mcp/sse"
